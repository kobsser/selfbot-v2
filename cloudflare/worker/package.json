{
  "name": "selfbot-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute selfbot-db --file=./schema.sql",
    "db:init:local": "wrangler d1 execute selfbot-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}